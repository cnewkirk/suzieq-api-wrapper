# Changelog

## [0.1.0] – 2026-03-08

Initial release.  21 table mixins with full verb coverage:

- **show** / **summarize** / **top** — all 21 tables
- **unique** — all tables except `devconfig`
- **assert** — `bgp`, `evpnVni`, `interface`, `ospf`
- **lpm** — `route`
- **find** — `network`

Tables: `address`, `arpnd`, `bgp`, `device`, `devconfig`, `evpnVni`,
`fs`, `interface`, `inventory`, `lldp`, `mac`, `mlag`, `namespace`,
`network`, `ospf`, `path`, `route`, `sqPoller`, `table`, `topology`, `vlan`.
